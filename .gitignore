_build
_tracks

opa-debug-js

*.opp
*.opx
*.opx.broken
*.dump
*.api
*.api-txt
*.exe
*.log

# ── umzhconnect-cow ──────────────────────────────────────────────────────────
# Local deployment config (copy from .env.example)
.env
# macOS folder metadata
.DS_Store
# L2 signing material — generated by keys/gen-keys.sh (and rotate.sh); NEVER
# commit a private key. Glob covers rotation kids (l2, l2-2026-08, …). The scripts
# (*.sh) and README stay tracked.
keys/*.key
keys/*.jwks.json

# ── tests ───────────────────────────────────────────────────────────────────
tests/reports/*.xml
