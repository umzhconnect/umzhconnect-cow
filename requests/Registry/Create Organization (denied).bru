meta {
  name: Create Organization (denied)
  type: http
  seq: 2
}

post {
  url: {{registryUrl}}/fhir/Organization
  body: json
}

headers {
  Content-Type: application/fhir+json
}

body:json {
  {
    "resourceType": "Organization",
    "name": "Should Not Be Created"
  }
}

docs {
  Writes to the registry are blocked at the proxy (limit_except GET HEAD).
  Expect 403 — the directory is read-only through this endpoint.
}
