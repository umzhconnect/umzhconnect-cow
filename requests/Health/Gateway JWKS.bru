meta {
  name: Gateway JWKS
  type: http
  seq: 2
}

get {
  url: {{gatewayUrl}}/jwks.json
}

docs {
  Public JWK Set for this party, proxied by the gateway to the key custodian.
  The auth server fetches this (via the client's jwks.url) to verify our
  private_key_jwt assertions.
}
